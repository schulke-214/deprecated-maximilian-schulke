// import { TimelineLite } from 'gsap';

import styles from './page-transition.scss';

const PageTransition = () => {
	return (
		<div id='__transition' className={`${styles.container}`}>
			<span>transition</span>
		</div>
	);
};

export default PageTransition;
