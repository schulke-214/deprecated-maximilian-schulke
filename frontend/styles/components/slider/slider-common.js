import css from 'styled-jsx/css';

export default css`
    div {
        z-index: 100;
        overflow: hidden;
        position: relative;
    }

    canvas {
        transform-origin: center;
        position: absolute;
    }
`;